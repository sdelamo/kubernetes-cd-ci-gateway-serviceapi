micronaut:
  application:
    name: gateway
  http:
    services:
      books:
        url: 'http://books'
endpoints:
  info:
    enabled: true
    sensitive: false
  health:
    enabled: true
    details-visible: ANONYMOUS
